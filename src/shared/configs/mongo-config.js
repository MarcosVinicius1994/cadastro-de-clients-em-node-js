const shared  = require('../configs/shared-variables.json')
module.exports = {
        host: shared.host,
        password: shared.password,
        user: shared.user,
        database: shared.database
}